@model Gremlin.ViewModels.QuizzesViewModel

<h2>
    @if (Model.UserId.HasValue)
    {
        <span>Quizzes created by User #@Model.UserId</span>
    }
    else
    {
        <span>All Quizzes</span>
    }
</h2>

<partial name="_QuizTable" model="Model" />